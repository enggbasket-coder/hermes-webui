import { Stub } from "@/components/Stub";
export default function Page() {
  return <Stub title="Memory" hint="Inline editors for SOUL.md / MEMORY.md / USER.md, and a session browser with FTS."
    cli="hermes -p <profile> memory  ·  hermes -p <profile> sessions list" />;
}
